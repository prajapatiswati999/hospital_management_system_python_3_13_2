import logging 
# Configure logging for the managers package 
logging.basicConfig(level=logging.INFO) 
logger = logging.getLogger(__name__) 
logger.info("Managers package initialized for hospital management system.")